import { Typography, Box } from "@mui/material";
export default () => (
  <Box sx={{ p: 3 }}>
    <Typography variant="h4">Welcome!</Typography>
    <Typography>Demo app with React Router & Context.</Typography>
  </Box>
);
